# SignSpeak
SignSpeak is a real-time Indian Sign Language translator using a custom CNN and OpenCV. Achieves 95.7% accuracy on a 2,600-image dataset and runs at 78.61 ms/frame, converting hand gestures into text with optional text-to-speech output.
